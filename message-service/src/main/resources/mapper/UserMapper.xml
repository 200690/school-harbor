<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.harbor.messageservice.mapper.UserMapper">

    <select id="selectByUserId" resultType="com.harbor.messageservice.domain.po.UserProfilePO">
        SELECT * FROM user_profile WHERE user_id = #{userId}
    </select>

    <delete id="deleteByUserId">
        DELETE FROM user_profile WHERE user_id = #{userId}
    </delete>

</mapper>
